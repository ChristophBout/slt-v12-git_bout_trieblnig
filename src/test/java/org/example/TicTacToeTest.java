package org.example;

import org.junit.jupiter.api.*;


import java.util.*;

class TicTacToeTest {
    /*
    @BeforeEach
    public void setUp() {
        originalIn = System.in;
    }

    @Test
    public void testPlayer1Wins() {
        String input = "Player1\nPlayer2\n0\n0\n0\n1\n0\n2\n1\n0\n1\n1\n1\n2\n2\n0\n";
        System.setIn(new ByteArrayInputStream(input.getBytes()));
        Scanner scanner = new Scanner(System.in);
        game = new TicTacToe(scanner);

        game.start();

        // Assuming that after winning, the game prompts for restart
        assertTrue(game.board.hasWinner(), "Player 1 should win the game");
    }

    @Test
    public void testGameTie() {
        String input = "Player1\nPlayer2\n0\n0\n0\n1\n0\n2\n1\n1\n1\n0\n1\n2\n2\n2\n2\n0\n2\n1\n";
        System.setIn(new ByteArrayInputStream(input.getBytes()));
        Scanner scanner = new Scanner(System.in);
        game = new TicTacToe(scanner);

        game.start();

        // Assuming that after a tie, the game prompts for restart
        assertTrue(game.board.isFull(), "The board should be full and the game should end in a tie");
        assertFalse(game.board.hasWinner(), "There should be no winner in a tie game");
    }

    @AfterEach
    public void restoreSystemIn() {
        System.setIn(originalIn);
    }

     */


}